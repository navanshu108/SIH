# Public build-time configuration only. Keep real secrets on a server.
VITE_API_BASE_URL=
VITE_WEATHER_API_KEY=
# HTTPS endpoint you own. It should call an AI provider and web-search tool server-side;
# never expose the provider key in this Vite application.
VITE_ASSISTANT_API_URL=
# Optional secure server endpoint for authorised NRSC/remote-sensing context.
VITE_NRSC_CONTEXT_API_URL=
